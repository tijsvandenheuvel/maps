// Fill this in with the config values from your Firebase project settings
// (Project settings > General > Your apps > SDK setup and configuration).
// This config is not a secret - it's meant to be public. Access control is
// enforced by the Firestore security rules and Google sign-in, not by hiding
// these values. See SETUP.md for the full setup walkthrough.
export const firebaseConfig = {
	apiKey: "YOUR_API_KEY",
	authDomain: "YOUR_PROJECT_ID.firebaseapp.com",
	projectId: "YOUR_PROJECT_ID",
	storageBucket: "YOUR_PROJECT_ID.appspot.com",
	messagingSenderId: "YOUR_SENDER_ID",
	appId: "YOUR_APP_ID",
};
